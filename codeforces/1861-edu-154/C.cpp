#include <bits/stdc++.h>
using namespace std;
int _;
string s;
int main() {
    ios::sync_with_stdio(false);
    cin>>_;
    while(_--) {
        cin>>s;
        for(auto op : s) {

        }
    }
    return 0;
}