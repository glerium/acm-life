#include <bits/stdc++.h>
using namespace std;
int t,n,q,tmp;
int main() {
    cin>>t;
    while(t--) {
        cin>>n>>q;
        while(n--) cin>>tmp;
        for(int i=1;i<=n-1;i++) cout<<i<<' '<<i+1<<endl;
        for(int i=1;i<=q;i++) cout<<
    }
}