#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    string s; cin >> s;
    int n = s.size();
    string t(s.rbegin(), s.rend());
    s = ' ' + s;
    t = ' ' + t;
    int f[n+5] = {};
    for(int i=1;i<=n;i++) {
        
    }
}